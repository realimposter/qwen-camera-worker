# Build and push Cog model to Replicate on every push to main.
# Uses replicate/setup-cog action which handles Docker + Cog installation.
# Uses jlumbroso/free-disk-space to reclaim ~30GB for the ~25GB model weights.

name: Push to Replicate

on:
  push:
    branches: [main]
    paths:
      - "predict.py"
      - "cog.yaml"
      - "requirements.txt"
      - "qwenimage/**"
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Check disk space
        run: df -h

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Cog
        uses: replicate/setup-cog@v2
        with:
          token: ${{ secrets.REPLICATE_API_TOKEN }}

      - name: Push to Replicate
        run: cog push r8.im/realimposter/qwen-camera-control
